#pragma once
// Copy to wifi_secrets.h (gitignored) and fill in lab credentials.
#define WIFI_SSID "your-ssid"
#define WIFI_PASSWORD "your-password"
// Jetson LAN or Tailscale IP running kineticpulse (TCP server :5555).
#define SERVER_IP "192.168.1.50"
#define SERVER_PORT 5555
